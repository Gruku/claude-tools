---
notes: 'Final integration: from a fresh empty directory run init-v3 → add epic + tasks
  → pick-task → spec-review → handover → end-session → start a new session → "continue
  this task" → log a lesson → log an issue → close issue via task completion → run
  an auto-epic. Capture any wiring gaps the unit tests missed. Pairs with v3-release-005
  (smoke-test against fresh install) but exercises the skill flow from a user perspective
  rather than the package install path.'
id: v3-skills-014
title: End-to-end skill walkthrough — fresh-project dogfood
---
