---
notes: Validate the subagent-isolation contract (orchestrator only sees ~50 tokens
  per task; per-task contexts are discarded), per-task summary aggregation (status
  / 1-paragraph summary / commit shas), auto-stage handover stub auto-archival on
  epic/phase completion, and the failure-aggregation policy (halt at first epic failure
  unless --continue-on-fail). Run a 5-task auto-epic dry-run on v3-skills epic itself
  (recursive dogfood).
id: v3-skills-011
title: Quality pass — auto-epic + auto-phase orchestrator skills
---
