---
notes: 'polish-013 documented 8+ different empty-state phrasings on the dashboard
  alone ("Nothing newly unblocked." / "No prior session yet." / "Tests: 0/0 passed"
  — last reads as data, not empty). Plus kanban "— filtered out —" lying about a non-existent
  filter, and several screens with no empty-state at all (lessons/issues filtered
  to zero, sessions search to zero, auto side-panels). Define a copy convention (tone,
  structure), build a shared <EmptyState> component, sweep all surfaces.'
id: v3-polish-019
title: Empty-state copy convention + shared component
---
