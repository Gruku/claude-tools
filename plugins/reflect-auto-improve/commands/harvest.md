---
description: Harvest recurring pain points and emit workflow proposals (always adversarial)
argument-hint: "[--project <path>] [--window <days>]"
---

Parse `$ARGUMENTS` and invoke the `reflect-auto-improve:harvest` skill.

- `--project <path>` → pass through; defaults to cwd.
- `--window <days>` → pass through; defaults to 14.
- No args → defaults.

Then call the skill.
