{
  "name": "gruku-tools",
  "owner": {
    "name": "gruku"
  },
  "metadata": {
    "description": "Claude Code plugins for AI task management, safety guard hooks, and UE5 material authoring",
    "pluginRoot": "./plugins"
  },
  "plugins": [
    {
      "name": "taskmaster",
      "source": "./plugins/taskmaster",
      "description": "Latest channel — v3 schema. Universal AI-powered task and backlog management plus narrative continuity: handovers, lessons, issues, recap, snapshot/PreCompact hook, auto-task / auto-epic / auto-phase state machines, per-task files. v2 backlogs keep working unchanged; opt into v3 via init or migrate. For the stable v2-only line, use the `master` branch. Requires: uv.",
      "version": "2.0.0-beta.1",
      "channel": "latest"
    },
    {
      "name": "guard-hooks",
      "source": "./plugins/guard-hooks",
      "description": "Safety guard hooks that block destructive CLI commands and sensitive file edits, with in-flow user approval via UserPromptSubmit. Requires: jq (https://jqlang.github.io/jq/)",
      "version": "2.2.2"
    },
    {
      "name": "ue5-materials",
      "source": "./plugins/ue5-materials",
      "description": "Author UE5 materials from YAML. Live mode drives the Material Editor via MCPBridge (localhost:13580); clipboard mode emits T3D paste text. HLSL Architect (Opus) + Node Generator (Sonnet) agent chain for complex designs, direct YAML for simple wiring. Requires: pyyaml.",
      "version": "3.0.1"
    },
    {
      "name": "statusline",
      "source": "./plugins/statusline",
      "description": "Pastel statusline for Claude Code with rate-limit bars, git status, context usage, update notifications, and session cost. Bash + PowerShell. Self-healing resolver survives plugin updates. Run /statusline:setup after install. Requires: jq (Bash only).",
      "version": "1.1.0"
    }
  ]
}
