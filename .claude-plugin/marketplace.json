{
  "name": "gruku-tools",
  "owner": {
    "name": "gruku"
  },
  "metadata": {
    "description": "Claude Code plugins for AI task management, safety guard hooks, and UE5 material authoring",
    "pluginRoot": "./plugins"
  },
  "plugins": [
    {
      "name": "taskmaster",
      "source": "./plugins/taskmaster",
      "description": "Universal AI-powered task and backlog management with narrative continuity: handovers, issues, areas, finite epics (done_when). Redesigned kanban viewer with inline edit-in-UI. Per-task files (.taskmaster/tasks/<id>.md) with slim YAML index. Lessons subsystem removed in 4.0 (durable knowledge now lives in assistant memory + CLAUDE.md/AGENTS.md) — run taskmaster:migrate-lessons once per project with existing lesson files; recap + project-structure scan removed in 4.2. v2 backlogs keep working unchanged; opt into v3 via taskmaster:migrate-v3 skill or backlog_init schema_version=3. Requires: uv (https://docs.astral.sh/uv/)",
      "version": "4.3.0"
    },
    {
      "name": "guard-hooks",
      "source": "./plugins/guard-hooks",
      "description": "Safety guard hooks that block destructive CLI commands and sensitive file edits, with user-approval override flow via AskUserQuestion. Pure-Python hooks (3.9+), no jq required.",
      "version": "2.8.0"
    },
    {
      "name": "ue5-materials",
      "source": "./plugins/ue5-materials",
      "description": "Author UE5 materials from YAML. Live mode drives the Material Editor via MCPBridge (localhost:13580); clipboard mode emits T3D paste text. HLSL Architect (Opus) + Node Generator (Sonnet) agent chain for complex designs, direct YAML for simple wiring. Requires: pyyaml.",
      "version": "3.0.1"
    },
    {
      "name": "statusline",
      "source": "./plugins/statusline",
      "description": "Pastel statusline for Claude Code with rate-limit bars, git status, context usage, update notifications, and session cost. Bash + PowerShell. Self-healing resolver survives plugin updates. Run /statusline:custom-statusline-install after install. Requires: jq (Bash only).",
      "version": "1.3.1"
    },
    {
      "name": "reflect-auto-improve",
      "source": "./plugins/reflect-auto-improve",
      "description": "Reflect on past work and propose concrete improvements. Time-windowed project retros, pain-point harvesting into workflow drafts, and claude-tools plugin self-audits. Outputs route through taskmaster as ideas and issues.",
      "version": "0.1.2"
    },
    {
      "name": "feedback-inbox",
      "source": "./plugins/feedback-inbox",
      "description": "Live feedback channel from any Claude instance back to the claude-tools toolmaker. Producer skill writes tagged messages to a configurable inbox; /inbox triage walks pending messages and routes accepted ones to taskmaster:add-idea or taskmaster:bug. Run /feedback-inbox-setup once per machine.",
      "version": "0.1.0"
    },
    {
      "name": "agent-relay",
      "source": "./plugins/agent-relay",
      "description": "Explicit, local-first direct and group communication between selected Codex and Claude Code CLI sessions, with direct messages, review requests, replies, and rooms.",
      "version": "0.3.0"
    }
  ]
}
